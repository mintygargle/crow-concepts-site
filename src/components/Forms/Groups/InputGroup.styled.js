import styled from "styled-components";
import { default as component } from "./InputGroup";

const InputGroup = styled(component)`
  width: 80%;
  margin-bottom: 0.6em;
`;

export default InputGroup;
